# Copy to config/mcp-env/labs.env (gitignored) and set a real value.
# Read by scripts/60-import-lab.sh, which fills __LAB_PASSWORD__ in every
# topology under labs/: the NX-OS admin user and the Ubuntu users. Pick
# something you are happy typing at a console. ADR 0006.
LAB_PASSWORD=change-me
